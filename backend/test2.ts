import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('orders').select('id, shift_id, created_at').limit(5).then(d => console.log(JSON.stringify(d, null, 2))).catch(console.error);
